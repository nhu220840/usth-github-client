<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.USTHGitHubClient"
        tools:targetApi="31" >
        <activity
            android:name=".AuthCallbackActivity"
            android:exported="false"
            android:theme="@style/Theme.USTHGitHubClient" />
        <!-- Activity receive redirect from browser -->
        <activity
            android:name=".auth.AuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.Transparent.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <!-- Match 100% with redirect URL that confirmed on GitHub: {scheme}://{host}/callback -->
                <data
                    android:host="@string/oauth_redirect_host"
                    android:path="/callback"
                    android:scheme="@string/oauth_redirect_scheme" />
            </intent-filter>
        </activity> <!-- Authentication main display (observe ViewModel, render UI) -->
        <activity
            android:name=".activities.AuthenticationActivity"
            android:exported="true"
            android:launchMode="singleTop" /> <!-- Launcher -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>